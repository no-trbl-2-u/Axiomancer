See [AGENTS.md](./AGENTS.md) for the monorepo guide, package layout, and hard
rules.

⛔ **Most important:** never read `/archive` unless explicitly told — it is the
retired nexus harness (stale memory, kept only as history + re-onboard seed).
`/carryforward` is staging, not a live `.claude/`.
