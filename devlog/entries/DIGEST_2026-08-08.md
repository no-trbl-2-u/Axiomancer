# 2026-08-08

> The queue shipped its first bounded balance phase in weeks — Phase
> 39 restored seven retired cards, reseated two preset identities, and
> retuned a fizzle gate — but the doctrine curve it was promoted to fix
> didn't move where it mattered: late-stage win rate is still a flat
> 0% and mid barely twitched (0.0% → 2.0%). The phase's own commit
> already said why (engine/enemy-scaling, not card composition); this
> digest files that as three owner-scoped findings instead of letting
> it quietly restate itself next cycle. Two other findings drained
> clean (a legibility gap, a test-coverage gap), critique pass 19 found
> one fresh comprehension bug, and every breadth leg is green.

## [balance] Phase 39 ships — but the doctrine-curve gate it was built to close stays red

**What:** restored 7 of D8's 10 retired cards (filling
peroration/forge/control/oracle/harvest's missing enchantment/
disenchant seats, `cardLibrary` 79 → 86), reseated `foundry` and
`grace`'s status-doctrine identity cards per owner ruling
(`entropy-tax` replaces the `mirror-of-longing` borrow; `heart-of-the-
matter` replaces the `ouroboros` borrow), resolved the resulting
color-law mismatch with a compensating enchantment-seat shuffle, and
retuned `ouroboros`'s `REPLAY_LAST` fizzle gate toward its ~25% target
(landed 11-17%).
**Why:** six consecutive nightly baselines (08-01 → 08-07) had read
byte-identical against doctrine (early ~80 / mid ~50 / late 25-35 /
impossible 0): early 61.1%, mid 0.0%, late 0%, impossible 0%. The
owner promoted one bounded phase via `/oversight` on 2026-08-08 to
carry the repair rather than let three overlapping candidates keep
restating the same signal.
**Commits:** `8d50591e` (phase, issue #177), `8eb33fb8` (build-plan
flip)

```diff
     if (forgedFloating.length > 0) dice = [...dice, ...forgedFloating];
-    // (`entropy-tax`'s kindled/floating-spend mark hook was retired with the
-    //  card in Phase D8's ten-in/ten-out promotion ledger.)
+    // `entropy-tax` (D): spending a KINDLED (temporary) or FLOATING die marks
+    // the enemy — the manufactured resource has a price (spec 32 v3 T3).
+    // Restored Phase 39 (2026-08-08) alongside the card (retired at D8).
+    if (zoneHas(state, 'entropy-tax')
+        && (poweringSource === 'floating' || (poweringSource === 'reserve' && powering.temporary))) {
+        const markDef = lookupEffectDef('debuff_mark');
+        if (markDef) {
+            const applied = applyEffect(enemy.effects, markDef, state.round, { intensityDelta: 1, sourceId: 'entropy-tax' });
+            enemy = { ...enemy, effects: applied.activeEffects };
+            events.push({
+                kind: 'effect-landed', cardId: 'entropy-tax', effectId: markDef.id, target: 'enemy',
+                effectKind: 'control', intensity: applied.result.activeEffect?.intensity ?? 1, effect: markDef,
+            });
+        }
+    }
```

**But the gate stayed red.** This digest's re-measure (reduced
nightly, committed at `8eb33fb8`) reads blind policy-pick early 61.1%
(unchanged) / mid 2.0% (up from 0.0% — 3 wins across 150 runs, the
first movement since 07-30, still deep in violation against ~50) /
late 0.0% (unchanged, zero movement on the phase's named target) /
impossible 0% (unchanged, correct). Phase 39's own shipping commit
already flagged this as a `needs-user-call`: presets that already
carried every proven staple still sat at 0% mid, which reads as
engine/enemy-scaling shaped, not card-composition shaped. Filed as
three `plan/AUDIT.md` rows this cycle (doctrine-curve confirmation +
foundry's early-stage regression + the mid-cliff needs-user-call) and
`plan/CRITIQUE.md`'s `[HIGH] late-stage global collapse` row updated
to record that Phase 39 landed without draining it. See Tuning
proposals below.

## [ui] Combat board surfaces the kill-path readout it always had the data for

**What:** forwarded mechanics' `projectCombatOutcome` selector
(pending DoT / rounds-to-kill / lethal-in-flight) onto
`CombatEnemyPaneVM` and rendered it on the combat board as a "DOT
PENDING" / "LETHAL IN N" readout, reusing the existing `AltWinMeter`
component slot.
**Why:** build-plan Phase 2 shipped the selector but nothing in
`axiomancer-mobile` ever consumed it — the status kill-path stayed
invisible to players exactly as `plan/CRITIQUE.md`'s "[LOW] Combat
kill-path legibility" row (audit score 2.8) described.
**Commits:** `615ff26b` (fix, issue #174), `1c915ac0` (audit finding
closed)

## [tests] GateSockets gets coverage a month after landing

**What:** 9 colocated tests for `GateSockets.tsx` (the Gate of Assent
/ Foundation socket UI) pinning socket rendering, the `distinctWords`
pocket dedupe, lay/take-back/clear behavior, the `openSockets`
capacity guard, and submit ordering.
**Why:** an `/iterate` audit sweep found the component owned real
interactive state with zero coverage since it landed 2026-07-07, and
the labyrinth e2e suite never touches `testID="labyrinth-gate"`.
**Commits:** `10048947` (test, issue #175), `1d2917e5` (audit finding
closed)

## While you were out

| Tick (UTC)             | Verb                     | Outcome |
|-------------------------|--------------------------|---------|
| 08-07 13:44 → 13:58      | `/march` → `/iterate`    | shipped — combat kill-path legibility (#174) |
| 08-07 19:25 → 19:45      | `/march` → `/iterate`    | shipped — GateSockets test coverage (#175) |
| 08-07 ~21:08 (attended)  | `/oversight`             | promoted Phase 39 + Phase 40, drained 4 needs-user-calls (#176) |
| 08-08 02:26 → 02:38      | `/march` → `/critique`   | pass 19 — 1 finding (exploration hub regionProgress mismatch) |
| 08-08 07:26 → 08:52      | `/march` → `/ship-a-phase` | shipped — Phase 39 curve repair + library theme-symmetry (#177) |

- `loop:phase` GitHub issues (the phase mirror): unchanged cadence,
  no stale-open rows found this sweep.
- `plan/AUDIT.md`: 3 new Pending rows filed this cycle (doctrine-curve
  confirmation + 2 needs-user-call rows mined from Phase 39's own
  commit body); 2 rows drained since last digest (#174, #175).
- `plan/CRITIQUE.md`: pass 19 today (commit `18eb0ddb` → `469a5694`),
  1 fresh MED finding (exploration hub static/dynamic node-count
  mismatch); the standing `[HIGH] late-stage global collapse` row
  updated to record Phase 39 landing without draining it.
- `plan/PHASE_CANDIDATES.md`: 34 pending candidates; the "Post-D8
  flag-on curve repair + library theme-symmetry" candidate moved to
  Promoted (now Phase 39, shipped).
- `triage:needs-user` / `loop:do` issues: 0 open. No open PRs.
- Deploy: nothing to check yet — HEAD is inside the grace window, no
  gated `verify-*` workflow triggered for this digest commit's paths.
- Breadth check (`npm --workspace axiomancer-mobile run e2e:minigames`)
  ran full and green: hazard, combat, gathering, encounter-routing,
  exploration/combat-prelude round-trip, and upgradeable-dice all
  `ALL PASS`.
- Deck-matrix baseline re-measured this cycle — see Tuning proposals.

## Needs you

Three new `[needs-user-call]` rows filed to `plan/AUDIT.md` this
cycle, all routed to `/oversight` (owner-scoped calls, not `/iterate`
picks):

1. **Doctrine-curve confirmation post-Phase-39** — the mid/late
   collapse the phase was promoted to fix is still in violation
   (late 0.0%, mid 2.0% against ~50). Needs a decision on whether to
   scope a dedicated mid/late enemy-threat-scaling phase (manual
   engine constant, per the standing rail that this loop doesn't tune
   its own constraints).
2. **Foundry's early-stage regression (73% → 44%)** — a real,
   expected cost of the status-doctrine seat ruling per Phase 39's own
   commit, not a bug. Needs a call on whether to queue an authored
   win-condition card for foundry or accept the number as settled.
3. **Mid-cliff attribution** — Phase 39's duplication sweep found
   10/13 staple candidates already fully deployed pre-phase with no
   effect on mid win rate, pointing at engine/enemy-scaling rather
   than card composition. Same decision as #1; filed separately since
   it's the evidence trail, not the ask.

No `triage:needs-user` issues, no blocked build-plan rows — Phase 40
(card-text grammar + copy pass) is next in queue, unblocked now that
Phase 39 has landed.

## Tuning proposals

`baseline:check` opened this cycle **stale by exactly one
mechanics-source commit** (`8d50591e`, Phase 39 itself) — re-measured
with the reduced nightly pass (`npm run baseline:regen -- --runs=30
--confidence=reduced-nightly`), now committed at `8eb33fb8`.

This is the first baseline read to actually move since 07-30 — the
prior six were byte-identical because nothing in that window touched
`scoreCard` or `draftCombatDeck`'s construction path. Phase 39 does
touch both (card restorations, preset reseats, a fizzle-gate retune),
so a real diff was expected. Read (blind policy-pick, doctrine early
~80 / mid ~50 / late 25-35 / impossible 0): early 61.1% (unchanged),
mid 2.0% (up from 0.0%, 3 wins / 150 runs — real movement, but two
orders of magnitude short of the ~50 target), late 0.0% (unchanged —
the phase's named target didn't move at all), impossible 0% (unchanged,
correct).

No new tuning action from this digest — the repair already ran, and
its own commit correctly diagnosed why the gate stayed red (see
Needs you above). Filed as findings, not proposals, because the fix
this data points at is a manual engine constant (enemy HP/threat
scaling), which is explicitly outside `/deck-tuning`'s card-only
remit and outside this loop's authority to self-legislate. Full detail
in `plan/AUDIT.md` and the updated `plan/CRITIQUE.md` `[HIGH]` row.

## Today's intent

No baseline screenshots changed since the last digest, so no UI shots
this cycle. The headline number here isn't the two small findings
that drained clean — it's that a phase explicitly scoped and promoted
to fix a six-cycle-confirmed doctrine violation shipped clean, green,
and fully verified, and the violation is still there. That's not a
failure of execution (the phase's own commit reasoned through why,
correctly, in real time) — it's a signal that the next productive
move on this thread is an owner call on engine constants, not another
card-tuning pass. Filed as three findings rather than one, so
`/oversight` can act on the specific asks (scope a scaling phase?
queue foundry's win-condition card?) instead of re-deriving them from
a wall of prose.
