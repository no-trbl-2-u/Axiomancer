# 2026-07-13

> A dense overnight-plus-day window: Phase 31 ("The Roll and the Read" —
> momentum wheel goes engine-native, THE STAKE ships, signature dominance
> repriced) landed in three parts, Phase 29's status line got corrected to
> match reality, a seven-PR card-clarity audit thread closed out two
> full legibility passes (catalog faces + mobile overlay + engine wording),
> and a cleanup pass shipped attribution honesty and a scrap-spam gate.
> Breadth checks and deploy gate both green.

## [mechanics,mobile] Phase 31 — The Roll and the Read

**What:** the combat momentum wheel moved from a mobile-side state splice
into the engine (`advanceMomentumWheel`, gated on a landed `card-played`
event); a new **THE STAKE** action lets the player wager 2/4/6 Conviction
post-draft on the enemy's hidden stance, settling at the top of
`resolveThreatPhase` into a floating-die mint (win) or an escalation-clock
penalty (loss); and `sig-conviction-strike` was repriced 7→8 Conviction
after a 540-run stage matrix showed it took 83.6% of all signature casts,
crowding out `sig-overwhelming-argument` at the same affordability
threshold.
**Why:** closes build-plan Phase 31 (bundled per `01_build_plan.md`'s row,
issue #82) and, as a side effect of reusing the floating-die pool for the
wheel-granted die, likely fixes the standing HIGH `plan/CRITIQUE.md` finding
that the wheel's forged wild die couldn't be dragged onto any card and
silently burned as a spare — worth a `/iterate` confirm-and-drain pass.
**Commits:** a5df832b, 380e3849, 48c89f34, 040202fd, 610541d3

```diff
+export function placeStake(
+    state: CombatEncounterState,
+    color: WheelStance,
+    amount: 2 | 4 | 6,
+): CombatTransition {
+    if (state.phase !== 'phase-play') return { state, events: [] };
+    if (state.stake) return { state, events: [] };
+    if (state.draftedDieId === null) return { state, events: [] };
+    if (state.conviction < amount) return { state, events: [] };
+
+    const conviction = state.conviction - amount;
+    const events: CombatEvent[] = [{ kind: 'stake-placed', color, amount }];
+    const next: CombatEncounterState = { ...state, conviction, stake: { color, amount } };
+    return { state: withLog(next, events), events };
+}
```

## [mechanics] Phase 29 — correcting the record

**What:** the build-plan status line said Phase 29 (keyword-language
honesty pass) was PARTIAL; both workspaces' full verify gates (2496 +
2557 tests) confirm the cloud-loop commit + reconciliation merge already
shipped the full scope. Flipped the status line to shipped instead of
re-doing already-landed work.
**Why:** the earlier PARTIAL note was stale — re-touching
`keywords.ts`/`card-themes.ts`/the keyword atlas to "finish" work that
was already there risked duplicate or conflicting edits.
**Commits:** db9cceca

## [content] Card-catalog clarity audit — game-faithful faces (PRs #75-#78)

**What:** a four-PR thread closed the catalog exporter's fidelity gap:
regenerated stale artifacts, then swapped the catalog's bespoke card-text
re-derivation for the game's own `toCombatCard` renderer (failures
40/70 → 18/70), planned the residual 18 against real engine values
(13 "surface the value" + 5 needing an owner ruling), then implemented
both buckets plus a fresh-audit extra sweep — RUPTURE ALL glosses, real
percentages/multipliers on six cards, a Mirror of Guilt engine fix
(enemy-inflicted debuffs no longer reflect), and popup coverage for
**every** printed keyword (previously ~30 keywords had no popup at all).
**Why:** the catalog and the in-game overlay were both silently lying
about several cards' real numbers — a clarity doctrine violation the
owner scoped as "player must 100% know what a card does."
**Commits:** 5ad01930, 826ae7d6, 75d25735, f94685fb, 7320faee, 0b4dfe0c,
04104ddd

## [ui] Card-wording & overlay clarity (PRs #79-#80)

**What:** a headless-Chromium harness captured all 70 card overlays
across every starter deck; ten judge passes flagged the `NO-DIE` pill as
pure duplication of the card face and the `+DIE` pill as collapsing
multi-effect paid lines to one keyword. Fix: deleted the `NO-DIE` pill,
widened `+DIE` to the full authored paid line, registered `MILL`
(previously printed on 3 cards with no gloss anywhere), and added board
glyphs for attached enchantments (❖) and curses (☒) — "the single most
consequential legibility gap," since no effect id had ever backed a
persistent passive's board presence.
**Why:** keyword panels were truthful but looked orphaned because the
pill above them hid the very effects they were defining.
**Commits:** 24b4d5bb, f9f91a7a

## [content] Card-clarity audit round 2 — every flagged face self-explains (PR #81)

**What:** fixed all 14 findings from a fresh catalog-only audit (face +
keyword atlas, no code read): riders now name what they boost, RUPTURE's
bonus always prints "damage" (never the undefined "fuel"), CONCEDE
prints "you win", the two non-registry DoTs print their bite inline, and
three ambiguous `persistentEffect` strings now name their own mechanics.
Established one wording rule: an effect clause names its side only when
it crosses the card's own printed target.
**Why:** extends the 2026-07-12 wording audit to the engine-generated
face text itself — text every carrier shares, so each fix repairs every
occurrence of that pattern at once.
**Commits:** 91f7ecb2

## [mechanics] Cleanup pass — attribution honesty, scrap gate, legibility sweep

**What:** `buildCombatSummary` now sums enemy DoT from emitted tick
events instead of projecting a DoT's whole remaining life (was
over-attributing damage to the wrong source); `discardCombatCard` caps
free Conviction-refund scraps at 2/turn before further scraps cycle for
free (closes a scrap-spam exploit); a drag-interrupt-recovery hook was
added for the card tray; and a `debuff_petrify` library tag was
backfilled to satisfy the v3 deprecation contract after a same-day
verify-mechanics failure.
**Why:** completes `plan/cleanup/Clean-up-pass-07-11.md`'s WI-9/WI-10 +
P2 legibility items (HP→VITAE sweep, RESONANCE gloss, a11y states).
**Commits:** b097efec, 88406af8, cc65ce4d, 9e75c2dc, 1101f660

## [infra] Architecture map + CI trim

**What:** added `docs/external-architecture.md` mapping Axiomancer's
external surfaces; removed `preview-build.yml`'s daily 09:15 EST cron
(scheduled EAS builds are now attended-dispatch only).
**Why:** cut Actions minutes spent on unattended EAS builds nobody was
consuming; the architecture doc gives the loop one map instead of
scattered tribal knowledge.
**Commits:** cc7f7a25, f8f47424

## While you were out

| Time (UTC) | Verb | Outcome |
|---|---|---|
| 2026-07-12 19:35 → 19:45 | march (scheduled) | tick succeeded |
| 2026-07-12 (window) | owner/cloud sessions (direct commits + PRs #75-#81) | catalog + overlay + card-clarity audit thread; cleanup pass; architecture doc; CI trim |
| 2026-07-13 04:06 → 04:24 | march (scheduled) | tick succeeded |
| 2026-07-13 09:51 → 11:04 | march (scheduled) | tick succeeded — Phase 29 correction + Phase 31 (3 parts) shipped |

Build-plan queue (`plan/steps/01_build_plan.md`): Phase 29 corrected to
shipped, Phase 31 shipped this window; Phases 32-34 pending, no
`[blocked:]` rows (32/33 route to `/deck-tuning`; 34 is the
non-Playwright critique transport). `plan/AUDIT.md`: 9 pending — row
[3.2] (night workflow never installs Playwright's browser) is unchanged
for a **fifth** digest running, worked around by hand again this run;
it is tied for the highest score in the file (3.2) yet keeps losing to
pending-phase work under `/march`'s "deliveries first" dispatch order
(`skills/march.md` §: pending phase ships before `/iterate`) — not a
mistuning, just a real cost of a phase-heavy window; flagged below.
`plan/CRITIQUE.md`: 25 pending (up from 19 two digests ago) — none
drained this window for the same reason; 8 of the 25 are HIGH. One,
"momentum wheel's forged wild die cannot be spent," was very likely
fixed as a side effect of Phase 31 part 1 reusing the floating-die pool
— unconfirmed, needs a play-test to drain. `plan/PHASE_CANDIDATES.md`:
18 pending, 0 promoted (`/expand` has never run — pass count 0). No open
`triage:needs-user` or `loop:do` issues. Deploy gate: CI green — all 3
`verify-*` workflows succeeded for the last code commit (610541d3); HEAD
(4efbdfd4) is a plan-only commit that triggered no gated workflow.
Breadth check (`npm run e2e:minigames`, browser installed by hand):
hazard, combat, gathering, encounter-routing, exploration-roundtrip —
ALL PASS.

## Needs you

- **The night workflow still never installs Playwright's browser**
  (`plan/AUDIT.md` [3.2]) — a fifth consecutive digest working around it
  by hand. `impact 4 / ease 8`, tied for the file's top score; it keeps
  losing to pending-phase work, not to a scoring bug. Once the build-plan
  queue thins (Phases 32-34 route elsewhere), this should surface to
  `/iterate` on its own — flagging in case it doesn't.
- **`plan/CRITIQUE.md` pending count grew 19 → 25 across the last two
  digests, with zero drains.** Expected while phases are queued (`/march`
  ships phases before `/iterate`), but 8 of the 25 rows are HIGH,
  including two tuning-harness findings (draft-scorer pool-shuffle bug,
  late-stage 0.00 win-rate collapse) that block six pending A/B
  verdicts. Worth an explicit `/iterate` or `/oversight` pass once Phase
  31's dust settles.
- **Confirm-and-drain candidate:** Phase 31 part 1's floating-die reuse
  for the momentum wheel very likely fixes the HIGH CRITIQUE row about
  the forged wild die not being spendable (same drag/spend path every
  other float already uses) — nobody has verified this in the live UI
  yet.
- **Two design calls parked from the 2026-07-12 review closeout**
  (whether the three ratified direct-HP exception arms should feed
  BLEED's damage-instance clock; whether WS9's reactive cleanse should
  shave intensity instead of removing a whole merged instance) are still
  waiting on an owner call.
- **The combat-fork DESIGN SIGNAL and the early-game preset-deck
  rethink** (parked in `plan/CRITIQUE.md` since prior passes) are still
  waiting on a `/brainstorm-mechanics` or `/oversight` session.

## Tuning proposals

None filed by digest this pass. The CRITIQUE backlog growth above is a
real signal but traces to `/march`'s documented "deliveries first"
dispatch order (pending phases ship before `/iterate`/`/critique`
drainage) rather than a mistuned gate or ceiling — no constraint change
to propose; noted under Needs you instead so a human can judge whether
the phase-heavy queue should be re-sequenced.
