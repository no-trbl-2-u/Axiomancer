# 2026-09-07

> `adjust-npcs` debuts and closes the content-lifecycle set — all five
> `/adjust-*` stewards have now logged a pass — while two owner sessions
> fix a red-hand starter deck and rebuild the `/dev` surface end to end.
> The nightly breadth check stays red for a **second** consecutive
> night on the same already-diagnosed regex bug (still unshipped, not
> new). A fresh reduced-nightly measurement reads a lower late/higher
> impossible terminal rate than 2026-09-05 — directional only.

## [content] `adjust-npcs` pass 1 — the fifth and final content-lifecycle category debuts

**What:** Staged the Forest Ranger and the Lost Trader, unblocking the
`get-to-cave` route.

**Why:** The 2026-09-05 digest flagged `npcs` as the one `/adjust-*`
category still reporting "never." This pass closes it — cards,
equipment, enemies, keywords, and npcs have now all logged at least
one pass since the per-item content-lifecycle split (Phase 132-era,
2026-09-03).

**Commits:** `f0a2891f`, `a6d6679d`

## [mechanics] Owner session: tri-colour starter deck, stop de-duping the card base

**What:** `STARTING_CARD_IDS` grew from two BODY-only cards to one
card per stance (BODY/MIND/HEART), and `buildCombatDeck` stopped
collapsing every authored copy count to one.

**Why:** A live combat playthrough (instrumented `combat-round-e2e`)
found two independent defects: the `ensureStarterCards` fallback dealt
a 100%-BODY hand off a two-card deck (HEART/MIND dice dead on
arrival), and de-duping meant the 18-card Threadbare Office was dealt
as an 8-card deck — below the deck-removal floor. Re-measured the
stage-profile matrix at matched settings before/after: byte-identical,
because that harness builds decks from unique ids and never saw the
de-dup repeal.

```diff
-/** The cards a brand-new player starts with: an opening offensive card PLUS a
- *  basic defense card, so every player can GUARD from turn one. The rest unlock
- *  through ethical-dilemma events via `unlockCardViaDilemma`. The mobile
- *  bootstrap seeds a new character's `knownCards` from this list. */
+/**
+ * The cards a brand-new player starts with — an opening offensive card PLUS a
+ * basic defense card, so every player can GUARD from turn one, and one card in
+ * EACH of the three stance colours. The rest unlock through ethical-dilemma
+ * events via `unlockCardViaDilemma`. The mobile bootstrap seeds a new
+ * character's `knownCards` from this list.
+ *
+ * THE COLOUR LAW APPLIES TO THE STARTER DECK TOO (playthrough report
+ * 2026-09-05). This list used to be `spoiled-poultice` + `chilblain-watch`,
+ * and BOTH are `philosophicalAspect: 'body'` ...
```

**Commits:** `3fb4963b`

## [infra] Owner session: `/dev` surface audited and rebuilt

**What:** Retired nine stale/duplicated debug controls (a retired
loot-rarity model, dead encounter buttons, raw-`setState` playthrough
presets, seed/Aporia/map-reset buttons, synthetic dialogue/quest
fixtures) and added a live STATE inspector, world travel to any
map+node, `COMPLETE MAP`, labyrinth acts I-III, an any-foe enemy
picker, reward triggers, real NPC dialogue/quest pickers, and buff/
debuff/item pickers. CI-facing testIDs were preserved.

**Why:** Phase 132's extraction kept a stale, duplicated `/dev` route
pending a later audit; this pass makes every late-game surface
(reward channels included) reachable in a tap.

**Commits:** `e3807ae8`

## [content] `adjust-cards` pass 2 — 2 created, CHAIN/OMEN second carriers

**What:** Authored "The Village Comes Over the Hill" (rank 4/Rib,
heart) and "The Ducking Stool" (rank 3/Splinter, body) as second
carriers for CHAIN and OMEN, each of which had exactly one carrier
per `/adjust-keywords` pass 1's rider-inclusive audit.

**Why:** Both keywords anchor a load-bearing atlas family (the damage
octet; tempo-and-control) rather than being accidental cruft, so the
loop-call called for a second carrier, not a retirement.

**Commits:** `8c346ac7`, `4113dc0d`

## [content] `adjust-equipment` pass 2 — zero-action re-audit

**What:** Re-ran the full structural sweep over relics/consumables/
shop pools; nothing needed shipping this pass beyond a ledger sha fix.

**Why:** Standing rotation cadence — a clean re-audit is itself a
signal (no drift since pass 1's five dead-consumable rewires).

**Commits:** `721adac9`, `5ff070d7`

## [balance] Baseline re-measured — directional stage read

**What:** The SessionStart hook flagged the standing baseline
(`42cc0412`, reduced-nightly) stale by 3 mechanics-source commits.
Re-measured via the reduced-nightly pass (30 runs/cell) at commit
`5ff070d7`. Stage terminal win rates across the 144-cell matrix:
**early 97.3%, mid 95.4%, late 73.9%, impossible 35.4%**. Overall
combat-quality index 0.73 (per-stage 0.69/0.70/0.76/0.74 — read-only,
not a governing score).

**Why:** This digest's own freshness gate exists so a balance question
never gets answered against a stale engine, and this window carried
two real mechanics-adjacent touches (the starter-deck/de-dup fix, two
new CHAIN/OMEN cards).

**Note:** Late and impossible read notably different from the
2026-09-05 measurement (86%/26% → 73.9%/35.4%) — a 12pp late drop and
a 9pp impossible rise. This is a single-seed, reduced-confidence read;
not cited as a regression or filed as a `/deck-tuning` candidate from
here — measure and report only, per this skill's own rail — but it's
a large enough swing that a confirming full 3-seed `baseline:regen`
looks worthwhile before anyone treats it as settled.

**Commits:** `5ff070d7` (baseline regen, this tick)

## While you were out

| when (UTC) | verb | outcome |
|---|---|---|
| 2026-09-05 15:19–15:53 | march → adjust-npcs | success — pass 1, Forest Ranger + Lost Trader staged |
| 2026-09-05 18:22–18:25 | march | success, no-op |
| 2026-09-05 ~20:48 (PR #288) | owner session | tri-colour starter deck + stop de-duping the card base |
| 2026-09-05 23:57–00:02 | march | success, no-op |
| 2026-09-06 01:07–01:16 | march → critique | success — pass 32, 0 findings |
| 2026-09-06 02:36–02:42 | march | success, no-op |
| 2026-09-06 04:31–04:37 | march | success, no-op |
| 2026-09-06 ~06:13 (PR #289) | owner session | `/dev` surface audited and rebuilt |
| 2026-09-06 06:40–06:44 | march | success, no-op |
| 2026-09-06 08:32–08:38 | march | success, no-op |
| 2026-09-06 10:26–11:14 | march → adjust-cards | success — pass 2, 2 created, CHAIN/OMEN second carriers |
| 2026-09-06 12:35–12:38 | march | success, no-op |
| 2026-09-06 14:26–14:36 | march → adjust-equipment | success — pass 2, zero-action re-audit |
| 2026-09-06 16:28–20:32 | march ×3 | success, no-op |
| 2026-09-06 22:25–22:34 | march | success, no-op |
| 2026-09-07 01:07–12:41 | march ×7 | success, no-op |

All 20 `march` runs this window succeeded — no crashes, no cancelled
runs. Both non-loop changes (the starter-deck fix, the `/dev` rebuild)
shipped through owner-directed PR sessions, matching the pattern the
last several digests have noted.

## Needs you

- **Nightly breadth check is still red, a second consecutive night**:
  `upgradeable-dice-e2e`'s `SWAY-commit guard` hard-fails with the same
  message as 2026-09-05 (`no usable heart/wild die in the opening tray
  this seed`). This is the same already-diagnosed bug from the "Die
  a11y copy drift" row (filed 2026-09-04, escalated 2026-09-05): the
  script's `color`/`usable` die-label regexes are stale against the
  current a11y wording. No commit touched the script this window, so
  it's unfixed, not regressed. Fix is scoped in `plan/AUDIT.md`: port
  `combat-round-e2e.mjs`'s current regex shape in. Every other breadth
  leg (hazard, combat-encounter, encounter-routing, exploration/combat
  round-trip) passes clean; the pipeline aborts before `combat-round-e2e`
  (MODE=both) ever runs.
- **Late/impossible terminal-rate swing** (86%/26% → 73.9%/35.4% since
  2026-09-05) — directional only, flagged above, not filed.
- **[MED] LOG toggle overlaps the stance-check telegraph** — critique
  pass 32 reconfirmed unchanged (same root cause, collision line
  shifted one line up from pass 31's read).
- Five standing `[loop-call]` rows in `plan/AUDIT.md` still await an
  owner or mechanics-expert pick: no mid/late equipment progression,
  19 dead `zoneHas` engine hooks, a missing TWIN trigger on
  the-sextons-count, the Phase 78 W5 art-pass candidates, and the
  Phase W3 northern-city design decisions.
- No open `triage:needs-user` or `loop:do` issues.

## Tuning proposals

None from this digest's own meta-loop rail this window — the pulse
shows a healthy content-lifecycle rotation (all five categories now
active) and no starved queue or mistuned gate signal. `/expand` last
ran 2026-09-03 (pass 11, 3 candidates); no new pass fell in this
window.

## Queues now

- Build plan (`plan/steps/01_build_plan.md`): 0 open phases (`[ ]`),
  11 partial (`[-]`), 7 explicitly `[skipped]`, 124 shipped through
  Phase 78 — unchanged this window.
- `plan/AUDIT.md`: 67 rows under "Pending" excluding 46 resolved-but-
  unmoved `[x]` bookkeeping rows (a housekeeping gap, not new this
  window) — includes the newly-updated breadth-check row above.
- `plan/CRITIQUE.md`: 29 rows under "Pending"; last full pass
  2026-09-06 (pass 32, 0 new findings, fully clean re-confirmation).
- `plan/PHASE_CANDIDATES.md`: 52 rows under "Pending", unchanged —
  last `/expand` pass (11) was 2026-09-03, before this window opened.
- `plan/CONTENT_LEDGER.md`: cards + equipment now pass count 2;
  enemies + keywords + npcs at pass count 1 — every category active.
- No open `triage:needs-user` or `loop:do` issues.
- Deploy: clean — HEAD is a docs/plan-only tick, no gated workflow
  triggered.
- Nightly breadth (`e2e:minigames`): **RED**, second consecutive night
  — see Needs you.
- Measurement: was STALE at session start (baseline `42cc0412`, 3
  mechanics-source commits behind); re-measured this tick via
  `npm run baseline:regen -- --runs=30 --confidence=reduced-nightly` →
  now FRESH at `5ff070d7`. See the `[balance]` card above.
