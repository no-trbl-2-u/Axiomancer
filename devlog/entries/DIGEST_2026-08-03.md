# 2026-08-03

> A quiet infra night — two long-standing gaps closed at the root: the
> night runner's cold-Playwright breadth-check failure (ten nights
> running) and `docs/combat.md`'s description of a screen that was
> deleted two months ago. Both landed clean, all six breadth-check
> legs pass, and the deck-matrix baseline is still fresh — no re-measure
> needed. The `Closes #N` no-op keeps recurring (thirteenth confirmed
> instance) and stays unpicked-up. Pulse review also caught and closed
> a stale duplicate issue.

## [infra] Night workflow now installs Playwright before the breadth check

**What:** `.github/workflows/night.yml` now passes
`install_playwright: true` to the shared skill workflow, mirroring
`critique.yml` and its peers.
**Why:** without it, `/digest`'s `e2e:minigames` breadth check aborted
on every cold Actions runner ("Executable doesn't exist at
`.../chromium_headless_shell-1228/...`"), confirmed unfixed for ten
consecutive nights — each one needing a by-hand `npx playwright
install chromium-headless-shell` workaround before the check could even
run. `plan/AUDIT.md` finding `[3.2]`, issue #165.
```diff
--- a/.github/workflows/night.yml
+++ b/.github/workflows/night.yml
@@ -32,3 +32,4 @@ jobs:
         --dangerously-skip-permissions
       prompt: '/digest'
       timeout_minutes: 90
+      install_playwright: true
```
**Commits:** ca5b86f1 (issue #165 — see the `Closes #N` card below for
why this needed a hand-close anyway)

## [infra] `combat.md` rewritten for the current Hazard-Pattern Combat screen

**What:** replaced `docs/combat.md`'s description of the fully-retired
legacy combat screen (deleted `app/(tabs)/combat.tsx`, the removed
`resolveCombatRound` four-phase loop, a deleted `SkillConfirmOverlay`,
three e2e paths that no longer exist) with the real
`<CombatEncounterPanel>` / `<CombatBoard>` architecture — entry points,
data flow, the `CombatEncounterPhase` union, the drag-to-power turn
flow, board layout, `CombatViewModel` shape. Spec 04 marked superseded
so it isn't mistaken for current architecture.
**Why:** `plan/CRITIQUE.md`'s standing "Engine doc-drift is chronic" /
"Wrong-engine mental model in docs" rows (external-critique, score
~4.9) — a fresh reader following the old doc would have built a
mental model of a screen that hasn't existed since 2026-06.
```diff
-The combat screen lives at [`app/(tabs)/combat.tsx`](../app/(tabs)/combat.tsx)
-as a thin UI shell. All math, RNG, and state shaping happens in the
-presenter at [`state/presenters/combat.engine.ts`](../state/presenters/combat.engine.ts).
+> **Hazard-Pattern Combat (mechanics Spec 25, mobile Spec 26 / 26b) is the
+> ONLY combat engine.** The legacy turn-based resolver this doc used to
+> describe (`resolveCombatRound`, the four-phase `choosing_stance` loop,
+> `app/(tabs)/combat.tsx`) was fully removed from the engine in 2026-06.
```
**Commits:** 6690c5d0 (issue #164; verify: `npm run verify --workspace
axiomancer-mobile`, 268 suites / 2715 tests green)

## [infra] Thirteenth `Closes #N` no-op — still unpicked-up

**What:** #165's shipping commit (`ca5b86f1`) carried a correctly
numbered `Closes #165` trailer on a direct push to `main` — a
workflow-config-only diff with no verify gate to apply — and still
didn't auto-close the issue; closed by hand same-session.
**Why:** the same unexplained symptom tracked since #83 (Phase 32 Part
1, 2026-07-13). Thirteen confirmed instances now (#83, #151, #155,
#129, #156, #157, #158, #159, #160, #162, #163, #164, #165), no new
root-cause signal this time either. Already escalated to the top of
`plan/PHASE_CANDIDATES.md` (score 8.5, `[score 8.5, escalated]`) via
`/expand` — this entry is a log update, not a new proposal; the loop
doesn't get to fix its own constraint-tracking without `/oversight`.

## While you were out

| Tick (UTC)              | Verb              | Outcome |
|--------------------------|-------------------|---------|
| 08-02 14:04 → 14:22       | `/march` → `/iterate` | shipped — combat.md rewrite (#164) |
| 08-02 19:40 → 19:45       | `/march`            | no-op |
| 08-03 04:03 → 04:07       | `/march`            | no-op |
| 08-03 10:10 → 10:27       | `/march` → `/iterate` | shipped — night.yml Playwright fix (#165) |

- Build plan: 57/57 checked, none blocked.
- `plan/AUDIT.md`: 31 open pending rows (42 total under Pending, 11
  already marked resolved-in-place).
- `plan/CRITIQUE.md`: 21 open pending rows; last full pass 2026-07-31
  at commit `9a445281` (three days stale — next `/critique` due).
- `plan/PHASE_CANDIDATES.md`: 34 pending candidates.
- `triage:needs-user` / `loop:do` issues: 0 open (closed the one
  stale duplicate below).
- Deploy: nothing to check — HEAD is a docs/plan-only tick, no gated
  workflow triggered.

## Needs you

- Nothing blocked this cycle. One piece of hygiene handled directly:
  issue #144 ("title screen wordmark cropped above the mobile fold")
  was a duplicate of #148 — identical critique-pass-12 finding,
  already fixed by commit `0725b9cc` (2026-07-22), whose `Closes #148`
  trailer never touched the duplicate. Verified the fix
  (`contentFit="contain"`) is still live in `TitleScreen.tsx`, commented,
  and closed #144 as not-planned/duplicate.

## Tuning proposals

None. `npm run baseline:check` reports **FRESH** — baseline `acc64eca`,
measured 2026-08-02, confidence `reduced-nightly`, no mechanics-source
commits since. No re-measure triggered, so no doctrine-curve bands to
report against this cycle.

## Today's intent

Breadth check (`npm --workspace axiomancer-mobile run e2e:minigames`,
the project's real substitute for this skill's stale `npm run e2e`
pointer — `plan/AUDIT.md`'s standing divergence finding) ran full and
green: hazard, combat, gathering, encounter-routing, exploration/
combat-prelude round-trip, and upgradeable-dice all `ALL PASS`. No
baseline screenshots changed since the last digest, so no UI shots
this cycle.
