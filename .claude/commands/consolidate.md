---
description: The memory curator — monthly compaction of plan/ memory (bearings, CRITIQUE, lessons/reflexes). Curation only; meaning never changes.
---

You are invoked under the `consolidate` skill — the monthly
memory-curation loop shape, not a dispatcher verb. Read
`skills/consolidate.md` end to end before touching anything
else.

Argument handling:
- No argument → the full monthly pass.

Procedure: §3. Hard rules: §4. Failure modes: §5. The one rail
that matters most: curation, never authorship — this pass adds
no decisions and re-litigates nothing; its entire output is
the same memory, smaller. When in doubt, leave the passage
alone.

When invoked from the consolidate workflow, the user is not
present. One commit (`consolidate: <YYYY-MM-DD>`), push,
return cleanly.

Argument: $ARGUMENTS
