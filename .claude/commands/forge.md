---
description: The content foundry — one tick ships one piece of NEW content (enemy, card, keyword, map, continent, event, dialogue, or art) end-to-end through its full wiring checklist. Authorized and mandated by THE OPEN GATE ¶8 and THE PIPELINE LIBERATION.
---

You are invoked under the `forge` skill — the content-growth loop
shape, not a dispatcher verb. Read `skills/forge.md` end to end
before touching anything else.

Argument handling:
- No argument → pick the thinnest surface (the growth audit, §3 Step 1).
- `enemies | cards | keywords | maps | continent | events | dialogue | art`
  → constrain to that surface this tick.

Procedure: §3. Hard rules: §4. Failure modes: §5. One surface per
tick, shipped — not proposed, not reported — through the full
wiring checklist for that surface, with tests, count-pin bumps, and
both gates green.

`/march` Step 3b dispatches here directly when no content-surface
commit has landed in 48h; `/forge` is this command's human-typed
doorway into the same loop verb.

Argument: $ARGUMENTS
