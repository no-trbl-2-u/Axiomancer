/**
 * The Expo-decouple seam for the splash screen (phase 47a). `export *`
 * to preserve the existing `import * as SplashScreen from '...'`
 * namespace shape. Phase 47d owns the actual swap.
 */
export * from 'expo-splash-screen';
