/**
 * Treasure & reward art — temp art pass (2026-07-06 drop). Alpha-matted so the
 * pieces float on any dark screen.
 */

/** The banded red chest, shut — the Reliquary as first seen. */
export const TREASURE_CHEST_CLOSED = require('./treasure-box-a.webp');
/** The pale ossuary casket — spoils ledgers outside the cache. */
export const TREASURE_CHEST_PALE = require('./treasure-box-re-b.webp');
/** The spilled gold hoard with the ewer — the claimed tally. */
export const TREASURE_GOLD_HOARD = require('./treasure2.webp');
/** Coins caught mid-fall — combat spoils. */
export const TREASURE_FALLING_COINS = require('./creeping-coin-small-a.webp');
