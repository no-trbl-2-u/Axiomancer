#!/usr/bin/env node
// Build the DevLog catalog as one self-contained HTML document:
//   - devlog/catalog.html   cards + enemies + effects, with inline CSS/JS
//
// The JSON is produced by `npm run catalog:export` (ts-node, reads the mechanics
// libraries + mobile art registries). This renderer is zero-dependency and only
// reads the committed JSON + copied art, so it can run on any host.
//
// Usage: node scripts/build-catalog.mjs   (or: npm run catalog:build)

import { readFileSync, writeFileSync, existsSync } from "node:fs";
import { fileURLToPath } from "node:url";
import { dirname, join } from "node:path";
import { escapeHtml, inline, page } from "./devlog-shell.mjs";

const ROOT = join(dirname(fileURLToPath(import.meta.url)), "..");
const DEVLOG = join(ROOT, "devlog");
const DATA = join(DEVLOG, "data");

const BACK = { href: "./index.html", label: "← Axiomancer" };

function load(name) {
  const path = join(DATA, `${name}.json`);
  if (!existsSync(path)) {
    console.warn(`catalog: missing ${name}.json — run "npm run catalog:export" first.`);
    return [];
  }
  return JSON.parse(readFileSync(path, "utf8"));
}

// Freshness stamp (2026-07-17) — the export records which engine tree the
// JSON was derived from (devlog/data/meta.json); render it on the page so a
// stale catalog is visible at a glance instead of masquerading as current.
const META = existsSync(join(DATA, "meta.json"))
  ? JSON.parse(readFileSync(join(DATA, "meta.json"), "utf8"))
  : null;
const STAMP = META
  ? ` · from engine source <code>${escapeHtml(META.commit)}</code> on ${escapeHtml(META.generatedAt)}`
  : "";
const FOOTER = `single-file catalog generated by <code>scripts/build-catalog.mjs</code>${STAMP}`;

const CATALOG_EXTRA_STYLE = `
.catalog-nav {
  position: sticky; top: 52px; z-index: 4; display: flex; flex-wrap: wrap; gap: 8px;
  margin: -4px 0 22px; padding: 10px 0; background: var(--bg); border-bottom: 1px solid var(--border);
}
.catalog-nav a {
  display: inline-block; padding: 6px 10px; border: 1px solid var(--border); border-radius: 999px;
  background: var(--card); color: var(--fg); text-decoration: none; font-size: 13px; font-weight: 700;
}
.catalog-nav a:hover { background: var(--row); }
.catalog-section { margin: 34px 0 52px; scroll-margin-top: 110px; }
.catalog-section > h2 { font-size: 24px; margin: 0 0 6px; }
.catalog-section .intro { color: var(--muted); margin: 0 0 14px; }

/* ── Card FACE — the #5 SIDE RAIL design (die/stance-coloured spine) ──────── */
.gcard { position: relative; }
.cface {
  position: relative; aspect-ratio: 5 / 7; overflow: hidden; background: #0c0a08;
  font-family: "IM Fell English", Georgia, "Times New Roman", serif;
}
.cface .cart { position: absolute; inset: 0; }
.cface .cart img { width: 100%; height: 100%; object-fit: cover; object-position: top center; display: block; }
.cface .cart.none { display: flex; align-items: center; justify-content: center; color: rgba(232,223,200,.35); font-size: 44px; }
.cface .ctint { position: absolute; inset: 0; background: var(--rc); mix-blend-mode: soft-light; opacity: .17; }
.cface .cscrim {
  position: absolute; left: 0; right: 0; bottom: 0; height: 80%; pointer-events: none;
  background: linear-gradient(to top, #06050a 20%, color-mix(in srgb, #06050a 82%, var(--rc)) 46%, rgba(6,5,4,.5) 66%, transparent);
}
.cface .crail {
  position: absolute; left: 0; top: 0; bottom: 0; width: 18px; z-index: 2;
  background: linear-gradient(var(--rc), color-mix(in srgb, var(--rc) 45%, #000));
  border-right: 1px solid rgba(0,0,0,.5);
  display: flex; flex-direction: column; align-items: center; justify-content: center;
}
.cface .crail .cvert {
  writing-mode: vertical-rl; transform: rotate(180deg);
  font-family: "Bebas Neue", "Arial Narrow", system-ui, sans-serif;
  font-size: 10px; letter-spacing: 2px; color: rgba(255,255,255,.9); white-space: nowrap;
  max-height: 100%; overflow: hidden;
}
/* ① the GIANT free-effect glyph with its intensity centred INSIDE it. */
.cface .cfree {
  position: absolute; left: 4px; top: 4px; z-index: 3; width: 60px; height: 60px;
  display: flex; align-items: center; justify-content: center;
  font-size: 54px; line-height: 1; color: var(--rc); text-shadow: 0 2px 5px rgba(0,0,0,.85);
  background: radial-gradient(circle at 48% 46%, rgba(6,5,10,.82) 40%, rgba(6,5,10,0) 72%);
}
.cface .cfree .cfreeval {
  position: absolute; inset: 0; display: flex; align-items: center; justify-content: center;
  font-size: 17px; font-weight: 600; color: #fff;
  font-family: "JetBrains Mono", ui-monospace, monospace; text-shadow: 0 1px 3px rgba(0,0,0,.95);
}
.cface .crarity {
  position: absolute; top: 7px; right: 7px; z-index: 2; background: rgba(8,7,6,.7);
  font-family: "Bebas Neue", "Arial Narrow", system-ui, sans-serif;
  font-size: 10px; letter-spacing: 1px; padding: 1px 6px; border: 1px solid;
}
.cface .cbtm { position: absolute; left: 30px; right: 12px; bottom: 12px; z-index: 2; }
.cface .cbtm .cname {
  font-family: "Pirata One", "IM Fell English", Georgia, serif; font-size: 23px; line-height: 1.02;
  color: #e8dfc8; text-shadow: 0 2px 6px #000;
}
.cface .cbtm .crule { height: 1px; background: rgba(232,223,200,.22); margin: 8px 0; }
.cface .cbtm .cpaid {
  display: flex; gap: 7px; align-items: flex-start;
  font-family: "IM Fell English", Georgia, serif; font-size: 13.5px; line-height: 1.35; color: #e8dfc8;
}
.cface .cbtm .cpaid .ccube { flex-shrink: 0; margin-top: 2px; }
/* ── Glyph legend — the silhouette sheet, generated from GLYPH_SHAPES ─────── */
.glyph-grid { display: flex; flex-wrap: wrap; gap: 12px; margin-top: 10px; }
.glyph-tile {
  width: 104px; padding: 10px 6px 8px; text-align: center;
  background: var(--card); border: 1px solid var(--border); border-radius: 10px;
}
.glyph-tile svg { display: block; margin: 0 auto 6px; }
.glyph-kws { font-size: 11px; letter-spacing: .6px; color: var(--muted); font-weight: 700; line-height: 1.5; }

.cface .cbtm .cpaid b {
  font-family: "Bebas Neue", "Arial Narrow", system-ui, sans-serif; font-weight: 700;
  font-size: 1.12em; letter-spacing: .5px; text-transform: uppercase; color: var(--rc);
}
`;

// Stance → rail colour (mirrors the app's DIE palette) and rarity frame.
const STANCE_COLOR = { body: "#d6543f", mind: "#4f7fd6", heart: "#9a5fd0", wild: "#d9b44a" };
const RARITY_META = {
  common: { label: "COMMON", color: "#8a8273", glow: 0 },
  uncommon: { label: "UNCOMMON", color: "#6b8eb0", glow: 0 },
  rare: { label: "RARE", color: "#9a6ad6", glow: 12 },
};
// Keyword vocabulary bolded inside the PAID sentence (the card overlay defines
// them; the face just flags that they ARE keywords). Longest-first so multi-word
// keywords win the match.
const KEYWORD_WORDS = [
  "STRIP BUFF", "HEAL SELF", "POISON", "BLEED", "MARK", "GUARD",
  "RIPOSTE", "STAGGER", "BACKFIRE",
  "SWAY", "REAP", "SOUL", "SOULS", "FORETELL", "PREMISE", "ECHO", "FORGE", "DRAW",
  "SIPHON", "RUPTURE", "ENCHANT", "DISENCHANT",
  "PROLONG", "RECALL", "PERORATION", "CONCEDE", "CAPITULATE", "THORNS",
  "TICK", "DOT", "CLEANSE", "PERORATE", "PIP", "PIPS",
  // 2026-07-16 (authored paid summaries) — registry words the list was missing.
  "KINDLE", "RECOIL", "FALLEN", "OMEN", "RAPPORT", "MILL", "HEAL", "REARGUE", "PREMISES",
  // 2026-08-06 (iterate — retired-vocabulary sweep) — DAMAGE, STUN, SLOW, BURN,
  // CONFUSION, SILENCE, REGEN, EXECUTE, COMPOUND, VULNERABLE, BARRIER, REPRISE
  // dropped: spec 32 v3 retired or never had these; bolding them implied a
  // keyword the glossary overlay cannot define (plan/CRITIQUE.md).
].sort((a, b) => b.length - a.length);

function boldKeywords(text) {
  if (!text) return "";
  const esc = KEYWORD_WORDS.map((w) => w.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"));
  const re = new RegExp(`\\b(${esc.join("|")})\\b`, "gi");
  // Split into keyword / non-keyword runs and escape each part before joining.
  const out = [];
  let last = 0;
  let m;
  while ((m = re.exec(text)) !== null) {
    if (m.index > last) out.push(escapeHtml(text.slice(last, m.index)));
    out.push(`<b>${escapeHtml(m[0])}</b>`);
    last = m.index + m[0].length;
  }
  if (last < text.length) out.push(escapeHtml(text.slice(last)));
  return out.join("");
}

function chipVal(chips, key) {
  const c = (chips || []).find((x) => x.k === key);
  return c ? c.v : "";
}

// Effect-shaped FREE-glyph silhouettes (owner directive 2026-07-16) — a copy of
// the mobile table in axiomancer-mobile/components/combat/glyphShapes.ts (the
// stance-cube precedent: duplicated per renderer). Keys are the face keywords
// exported as `face.freeKw`; entries are 24×24 filled paths, `eo` = evenodd.
const SHAPE_FLAME = { d: "M12 2 C14 6 18 8 18 13 C18 17 15 21 12 21 C9 21 6 18 6 14 C6 11 8 10 9 8 C10 11 11 10 12 8 C12 6 11 4 12 2Z" };
const SHAPE_DROPS = { d: "M6 4 C4 9 4 12 6 13 C8 12 8 9 6 4Z M12 8 C10 13 10 16 12 17 C14 16 14 13 12 8Z M18 4 C16 9 16 12 18 13 C20 12 20 9 18 4Z" };
const SHAPE_FLASK = { d: "M9 2 H15 V4 H14 V8 L18.5 17.5 C19.6 19.9 17.6 22 12 22 C6.4 22 4.4 19.9 5.5 17.5 L10 8 V4 H9 Z" };
const SHAPE_SKULL = { d: "M12 2 C7.3 2 4 5.4 4 9.4 C4 12.3 5.7 14.5 8 15.6 L8 20 H10.2 L10.2 17.2 H11.2 L11.2 20 H12.8 L12.8 17.2 H13.8 L13.8 20 H16 L16 15.6 C18.3 14.5 20 12.3 20 9.4 C20 5.4 16.7 2 12 2 Z M8.8 8 A2 2 0 1 0 8.8 12 A2 2 0 1 0 8.8 8 Z M15.2 8 A2 2 0 1 0 15.2 12 A2 2 0 1 0 15.2 8 Z", eo: true };
const SHAPE_BOLT = { d: "M13 2 L5 13 H10 L8 22 L19 9 H13 L15 2 Z" };
const SHAPE_CROSSHAIR = { d: "M12 3.5 A8.5 8.5 0 1 0 12 20.5 A8.5 8.5 0 1 0 12 3.5 Z M12 6 A6 6 0 1 0 12 18 A6 6 0 1 0 12 6 Z M12 9.25 A2.75 2.75 0 1 0 12 14.75 A2.75 2.75 0 1 0 12 9.25 Z M11 0.5 H13 V3 H11 Z M11 21 H13 V23.5 H11 Z M0.5 11 H3 V13 H0.5 Z M21 11 H23.5 V13 H21 Z", eo: true };
const SHAPE_SHIELD = { d: "M12 2 L21 5 V12 C21 17 17 21 12 22 C7 21 3 17 3 12 V5 Z" };
const SHAPE_HEART = { d: "M12 21 C5 16 3 12 3 8 A4.6 4.6 0 0 1 12 7 A4.6 4.6 0 0 1 21 8 C21 12 19 16 12 21 Z" };
const SHAPE_CARD = { d: "M7 2 H17 A1.5 1.5 0 0 1 18.5 3.5 V20.5 A1.5 1.5 0 0 1 17 22 H7 A1.5 1.5 0 0 1 5.5 20.5 V3.5 A1.5 1.5 0 0 1 7 2 Z M8 4.5 H16 V11 H8 Z", eo: true };
const SHAPE_HOURGLASS = { d: "M6 2 H18 V6 L13.5 12 L18 18 V22 H6 V18 L10.5 12 L6 6 Z" };
const SHAPE_SPARKLE = { d: "M12 2 L14.2 9.8 L22 12 L14.2 14.2 L12 22 L9.8 14.2 L2 12 L9.8 9.8 Z" };
const SHAPE_EYE = { d: "M12 5.5 C6 5.5 2 12 2 12 C2 12 6 18.5 12 18.5 C18 18.5 22 12 22 12 C22 12 18 5.5 12 5.5 Z M12 8.5 A3.5 3.5 0 1 0 12 15.5 A3.5 3.5 0 1 0 12 8.5 Z", eo: true };
const SHAPE_HEXAGON = { d: "M12 2 L21 7 V17 L12 22 L3 17 V7 Z" };
const SHAPE_DOWN_ARROW = { d: "M8 3 H16 V12 H21 L12 22 L3 12 H8 Z" };
const SHAPE_BURST = { d: "M12 1 L14 8 L21 7 L15 12 L21 17 L14 16 L12 23 L10 16 L3 17 L9 12 L3 7 L10 8 Z" };
const SHAPE_DIAMONDS = { d: "M12 1 L15 5.5 L12 10 L9 5.5 Z M12 14 L15 18.5 L12 23 L9 18.5 Z M5.5 7.5 L8.5 12 L5.5 16.5 L2.5 12 Z M18.5 7.5 L21.5 12 L18.5 16.5 L15.5 12 Z" };
const SHAPE_DIAMOND = { d: "M12 2 L22 12 L12 22 L2 12 Z" };
const SHAPE_BOOKMARK = { d: "M7 2 H17 V22 L12 17.5 L7 22 Z" };
const SHAPE_WAVES = { d: "M2 9 C4.8 5.8 8 5.8 11 8.8 C13.7 11.5 16.3 11.5 19 8.8 L22 10.3 C18.4 14.2 14.3 14.3 11 11 C8.4 8.4 6 8.6 3.6 11.3 Z M2 15 C4.8 11.8 8 11.8 11 14.8 C13.7 17.5 16.3 17.5 19 14.8 L22 16.3 C18.4 20.2 14.3 20.3 11 17 C8.4 14.4 6 14.6 3.6 17.3 Z" };
const SHAPE_CHEVRONS_UP = { d: "M12 3 L20 10 L17.2 12.2 L12 7.6 L6.8 12.2 L4 10 Z M12 11 L20 18 L17.2 20.2 L12 15.6 L6.8 20.2 L4 18 Z" };
const SHAPE_SPILL_CARDS = { d: "M3 6 L11 3 L14 12 L6 15 Z M10 9 H21 V21 H10 Z" };
const SHAPE_SPEECH = { d: "M3 4 H21 V16 H12 L7 21 V16 H3 Z" };
const SHAPE_MASK = { d: "M2 8 C5 6.5 9 6.5 12 8 C15 6.5 19 6.5 22 8 C22 13 19 16.5 15.5 16.5 C13.8 16.5 12.8 15.4 12 14.2 C11.2 15.4 10.2 16.5 8.5 16.5 C5 16.5 2 13 2 8 Z M6.2 10 A1.8 1.8 0 1 0 6.2 13.6 A1.8 1.8 0 1 0 6.2 10 Z M17.8 10 A1.8 1.8 0 1 0 17.8 13.6 A1.8 1.8 0 1 0 17.8 10 Z", eo: true };
const SHAPE_DROPLET = { d: "M12 2 C17 9 19 12 19 15.5 A7 7 0 0 1 5 15.5 C5 12 7 9 12 2 Z" };
const SHAPE_RETURN_ARROW = { d: "M14 3 A7 7 0 0 1 14 17 H11 V21 L4 15 L11 9 V13 H14 A3 3 0 0 0 14 7 H8 V3 Z" };
const SHAPE_CYCLE_ARROW = { d: "M12 3 A9 9 0 1 0 21 12 H18.5 A6.5 6.5 0 1 1 12 5.5 L12 9 L18 4.5 L12 0 Z" };
const SHAPE_STAGGERED_BARS = { d: "M4 4 H16 V7 H4 Z M8 10.5 H20 V13.5 H8 Z M4 17 H16 V20 H4 Z" };
const SHAPE_CRYSTAL = { d: "M12 2 L21 12 L12 22 L3 12 Z M12 7 L16.5 12 L12 17 L7.5 12 Z", eo: true };
const SHAPE_STONE = { d: "M8 2.5 H16 L21.5 9 L18.5 21.5 H5.5 L2.5 9 Z M12.2 4 L13.6 8 L10.8 12.2 L13.2 16.4 L11.4 20 L10.2 20 L11.8 16.5 L9.4 12.2 L12.2 8 L11 4 Z", eo: true };
const SHAPE_CHEVRONS_RIGHT = { d: "M5 3 L14 12 L5 21 L2.8 18.8 L9.6 12 L2.8 5.2 Z M12 3 L21 12 L12 21 L9.8 18.8 L16.6 12 L9.8 5.2 Z" };
const GLYPH_SHAPES = {
  BLEED: SHAPE_DROPS,
  POISON: SHAPE_FLASK, FESTER: SHAPE_FLASK, ACID: SHAPE_FLASK,
  BURN: SHAPE_FLAME, DOT: SHAPE_FLAME,
  FROSTBITE: SHAPE_CRYSTAL,
  DOOM: SHAPE_SKULL, CURSE: SHAPE_SKULL,
  SHOCK: SHAPE_BOLT,
  PETRIFY: SHAPE_STONE,
  GRACE_MOMENTUM: SHAPE_CHEVRONS_RIGHT,
  MARK: SHAPE_CROSSHAIR,
  BACKFIRE: SHAPE_RETURN_ARROW,
  RAPPORT: SHAPE_SPEECH,
  STUN: SHAPE_BURST, RUPTURE: SHAPE_BURST, THORNS: SHAPE_BURST,
  GUARD: SHAPE_SHIELD, BARRIER: SHAPE_SHIELD,
  HEAL: SHAPE_HEART, REGEN: SHAPE_HEART,
  DRAW: SHAPE_CARD,
  TICK: SHAPE_HOURGLASS, DURATION: SHAPE_HOURGLASS, PROLONG: SHAPE_HOURGLASS,
  CLEANSE: SHAPE_DROPLET,
  SOUL: SHAPE_SPARKLE,
  FORETELL: SHAPE_EYE, OMEN: SHAPE_EYE,
  REVEAL: SHAPE_MASK,
  PIP: SHAPE_HEXAGON,
  RECOIL: SHAPE_DOWN_ARROW,
  STAGGER: SHAPE_STAGGERED_BARS,
  REFRESH: SHAPE_CYCLE_ARROW,
  ENCHANT: SHAPE_DIAMONDS,
  CONVICTION: SHAPE_DIAMOND,
  PREMISE: SHAPE_BOOKMARK,
  SWAY: SHAPE_WAVES,
  INTENSITY: SHAPE_CHEVRONS_UP,
  MILL: SHAPE_SPILL_CARDS,
};

function glyphShapeSvg(shape, { size = 48, fill = "var(--rc)" } = {}) {
  return (
    `<svg viewBox="0 0 24 24" width="${size}" height="${size}" aria-hidden="true" style="opacity:.95">` +
    `<path d="${shape.d}" fill="${fill}"${shape.eo ? ' fill-rule="evenodd"' : ""}/>` +
    `</svg>`
  );
}

// A tiny isometric die (matches the app's stance cube) tinted the rail colour.
function cubeSvg(color) {
  return (
    `<svg class="ccube" viewBox="0 0 24 24" width="18" height="18" aria-hidden="true">` +
    `<path d="M12 3 L21 8 L12 13 L3 8Z" fill="${color}"/>` +
    `<path d="M3 8 L12 13 L12 21 L3 16Z" fill="color-mix(in srgb, ${color} 68%, #000)"/>` +
    `<path d="M21 8 L12 13 L12 21 L21 16Z" fill="color-mix(in srgb, ${color} 42%, #000)"/>` +
    `</svg>`
  );
}

// The #5 rail card FACE — ① giant FREE glyph + intensity, ② PAID sentence
// (keywords bolded), stance rail + vertical type label, name, rarity tag.
function cardFaceHtml(c) {
  const stance = (chipVal(c.chips, "Stance") || "body").toLowerCase();
  const rc = STANCE_COLOR[stance] || "#8a8273";
  const kind = (chipVal(c.chips, "Kind") || "spell").toLowerCase();
  const kindLabel = kind === "disenchant" ? "CURSE" : kind.toUpperCase();
  const typeLabel = `${stance.toUpperCase()} · ${kindLabel}`;
  const rarKey = (chipVal(c.chips, "Rank").match(/\((\w+)\)/)?.[1] || "common").toLowerCase();
  const rar = RARITY_META[rarKey] || RARITY_META.common;
  const face = c.face || {};

  const art = c.image
    ? `<div class="cart"><img loading="lazy" src="${escapeHtml(c.image)}" alt="${escapeHtml(c.name)}"></div>`
    : `<div class="cart none" role="img" aria-label="${escapeHtml(c.name)} (no art)">🂠</div>`;
  const fv = face.freeVal;
  const fvText = fv ? (/^\d+$/.test(fv) ? "+" + fv : fv) : "";
  // Effect-shaped silhouette when the keyword has one; text rune fallback.
  const shape = face.freeKw ? GLYPH_SHAPES[face.freeKw] : null;
  const glyphInner = shape ? glyphShapeSvg(shape) : escapeHtml(face.freeGlyph);
  const freeGlyph = face.freeGlyph || shape
    ? `<div class="cfree">${glyphInner}` +
      (fvText ? `<span class="cfreeval">${escapeHtml(fvText)}</span>` : "") +
      `</div>`
    : "";
  const paidLine = face.paid
    ? `<div class="cpaid">${cubeSvg(rc)}<span>${boldKeywords(face.paid)}</span></div>`
    : "";

  return (
    `<div class="cface" style="--rc:${rc}">` +
    art +
    `<div class="ctint"></div><div class="cscrim"></div>` +
    `<div class="crail"><div class="cvert">${escapeHtml(typeLabel)}</div></div>` +
    freeGlyph +
    `<div class="crarity" style="color:${rar.color};border-color:${rar.color}88">${rar.label}</div>` +
    `<div class="cbtm"><div class="cname">${inline(c.name)}</div>` +
    `<div class="crule"></div>${paidLine}</div>` +
    `</div>`
  );
}

// A live client-side filter box shared by the cards/enemies sections. It is
// scoped by data-target so the single catalog page never reuses duplicate IDs.
const CATALOG_JS = `
<script>
(function () {
  function wireFilter(input) {
    var targetId = input.getAttribute('data-target');
    var grid = targetId ? document.getElementById(targetId) : null;
    var note = input.parentNode ? input.parentNode.querySelector('.count-note') : null;
    if (!grid) return;
    var items = Array.prototype.slice.call(grid.children);
    var total = items.length;
    input.addEventListener('input', function () {
      var q = input.value.trim().toLowerCase();
      var shown = 0;
      items.forEach(function (el) {
        var hit = !q || (el.getAttribute('data-search') || '').indexOf(q) !== -1;
        el.style.display = hit ? '' : 'none';
        if (hit) shown++;
      });
      if (note) note.textContent = q ? shown + ' of ' + total + ' shown' : total + ' total';
    });
  }
  Array.prototype.forEach.call(document.querySelectorAll('.catalog-filter'), wireFilter);
})();
</script>`;

function searchbar(targetId, placeholder, total, noun) {
  return (
    `<div class="searchbar"><input class="catalog-filter" data-target="${escapeHtml(targetId)}" type="search" autocomplete="off" ` +
    `placeholder="${escapeHtml(placeholder)}"><p class="count-note">${total} ${noun}</p></div>`
  );
}

function artOrPlaceholder(image, cls, glyph, alt) {
  if (image) {
    return `<div class="${cls}"><img loading="lazy" src="${escapeHtml(image)}" alt="${escapeHtml(alt)}"></div>`;
  }
  return `<div class="${cls} none" role="img" aria-label="${escapeHtml(alt)} (no art)">${glyph}</div>`;
}

// Raw-stat rendering — compact key/value badges + spelled-out mechanic lines.
function chipsHtml(chips) {
  if (!chips || !chips.length) return "";
  return (
    `<div class="statrow">` +
    chips
      .map((c) => `<span class="stat"><span class="k">${escapeHtml(c.k)}</span>${escapeHtml(c.v)}</span>`)
      .join("") +
    `</div>`
  );
}
function linesHtml(lines) {
  if (!lines || !lines.length) return "";
  return `<ul class="mods">` + lines.map((l) => `<li>${escapeHtml(l)}</li>`).join("") + `</ul>`;
}
function searchText(name, chips, lines) {
  const parts = [name]
    .concat((chips || []).map((c) => `${c.k} ${c.v}`))
    .concat(lines || []);
  return escapeHtml(parts.join(" ").toLowerCase());
}

// ---------------------------------------------------------------------------
// Cards — name · image · card text
// ---------------------------------------------------------------------------
function renderCards(cards) {
  const grid = cards
    .map((c) => {
      const search = searchText(c.name, c.chips, c.lines);
      const rarKey = (chipVal(c.chips, "Rank").match(/\((\w+)\)/)?.[1] || "common").toLowerCase();
      const rar = RARITY_META[rarKey] || RARITY_META.common;
      const frame =
        `border-color:${rar.color}` +
        (rar.glow ? `;box-shadow:0 0 ${rar.glow}px ${rar.color}55` : "");
      return (
        `  <article class="gcard" style="${frame}" data-search="${search}">` +
        cardFaceHtml(c) +
        `<div class="body">` +
        chipsHtml(c.chips) +
        linesHtml(c.lines) +
        `</div></article>`
      );
    })
    .join("\n");

  return (
    `<section id="cards" class="catalog-section">\n` +
    `<h2>Cards</h2>\n` +
    `<p class="intro">Name, art, stance, tier, target, rank, learn level, and rules text.</p>\n` +
    searchbar("cards-grid", "Filter cards by name or text…", cards.length, "cards") +
    `\n<div class="grid-cards" id="cards-grid">\n${grid}\n</div>\n` +
    `</section>`
  );
}

// ---------------------------------------------------------------------------
// Enemies — image · name · stats · attacks (skills + how it fights)
// ---------------------------------------------------------------------------
function renderEnemies(enemies) {
  const grid = enemies
    .map((e) => {
      const search = escapeHtml((e.name + " " + (e.difficulty || "") + " " + (e.logic || "")).toLowerCase());
      const portrait = artOrPlaceholder(e.image, "portrait", "☠", e.name);

      const stats =
        `<div class="statrow">` +
        `<span class="stat hp"><span class="k">HP</span>${e.maxHealth}</span>` +
        `<span class="stat body"><span class="k">Body</span>${e.stats.body}</span>` +
        `<span class="stat mind"><span class="k">Mind</span>${e.stats.mind}</span>` +
        `<span class="stat heart"><span class="k">Heart</span>${e.stats.heart}</span>` +
        `</div>`;

      const skills = (e.cards || []).length
        ? e.cards
            .map(
              (s) =>
                `<div class="skill"><div class="sk-name">${inline(s.name)}</div>` +
                `<div class="sk-text">${inline(s.text || "")}</div></div>`
            )
            .join("")
        : `<p class="no-skills">No signature skills — fights with basic stance moves.</p>`;

      const hint = e.stanceHint ? `<p class="hint">“${inline(e.stanceHint)}”</p>` : "";

      return (
        `  <article class="ecard" data-search="${search}">${portrait}` +
        `<div class="body">` +
        `<h2 class="name">${inline(e.name)}</h2>` +
        `<p class="sub">Level ${e.level} · ${escapeHtml(e.difficulty || "")}</p>` +
        stats +
        `<div class="sec-label">Attacks</div>` +
        `<p class="fights">${inline(e.logicBlurb || e.logic || "")}</p>` +
        hint +
        skills +
        `</div></article>`
      );
    })
    .join("\n");

  return (
    `<section id="enemies" class="catalog-section">\n` +
    `<h2>Enemies</h2>\n` +
    `<p class="intro">Portraits, level, difficulty, stats, behavior doctrine, and signature skills.</p>\n` +
    searchbar("enemies-grid", "Filter enemies by name, difficulty, behaviour…", enemies.length, "foes") +
    `\n<div class="grid-enemies" id="enemies-grid">\n${grid}\n</div>\n` +
    `</section>`
  );
}

// ---------------------------------------------------------------------------
// Effects — name · glyph · what it does (grouped buffs / debuffs)
// ---------------------------------------------------------------------------
function renderEffects(effects) {
  // Card-face SILHOUETTE where the effect's registry keyword has one (drawn
  // dark on the effect-coloured tile); the board's text glyph otherwise —
  // not every effect has a shape, and the text rune is still the truth of
  // how an unshaped effect reads on the combat board.
  const row = (e) => {
    const shape = e.kw ? GLYPH_SHAPES[e.kw] : null;
    const inner = shape ? glyphShapeSvg(shape, { size: 30, fill: "#0b0d10" }) : escapeHtml(e.glyph);
    return (
      `  <div class="eff"><div class="glyph" style="--gc:${escapeHtml(e.color)}" role="img" ` +
      `aria-label="${escapeHtml(e.name)} glyph">${inner}</div>` +
      `<div><div class="name">${inline(e.name)}</div>` +
      chipsHtml(e.chips) +
      linesHtml(e.lines) +
      `</div></div>`
    );
  };

  const section = (label, list) =>
    list.length
      ? `<div class="sec-label">${label} · ${list.length}</div>\n<div class="eff-list">\n${list
          .map(row)
          .join("\n")}\n</div>`
      : "";

  const buffs = effects.filter((e) => e.type === "buff");
  const debuffs = effects.filter((e) => e.type === "debuff");

  return (
    `<section id="effects" class="catalog-section">\n` +
    `<h2>Effects</h2>\n` +
    `<p class="intro">${effects.length} status effects — the glyph is how each one reads on the combat board.</p>\n` +
    section("Buffs", buffs) +
    "\n" +
    section("Debuffs", debuffs) +
    `\n</section>`
  );
}

// ---------------------------------------------------------------------------
// Glyphs — the effect-silhouette legend, generated from the live GLYPH_SHAPES
// table so the sheet can never drift from what the card faces draw. Aliases
// that share one silhouette render as one tile listing every keyword.
// ---------------------------------------------------------------------------
function renderGlyphs() {
  const byShape = new Map();
  for (const [kw, shape] of Object.entries(GLYPH_SHAPES)) {
    if (!byShape.has(shape)) byShape.set(shape, []);
    byShape.get(shape).push(kw);
  }
  const tiles = [...byShape.entries()]
    .sort((a, b) => a[1][0].localeCompare(b[1][0]))
    .map(
      ([shape, kws]) =>
        `  <div class="glyph-tile" role="img" aria-label="${escapeHtml(kws.join(' / '))} silhouette">` +
        glyphShapeSvg(shape, { size: 56 }) +
        `<div class="glyph-kws">${kws.map(escapeHtml).join("<br>")}</div></div>`
    )
    .join("\n");
  return (
    `<section id="glyphs" class="catalog-section" style="--rc:#d9b44a">\n` +
    `<h2>Glyphs</h2>\n` +
    `<p class="intro">${Object.keys(GLYPH_SHAPES).length} keywords, ${byShape.size} silhouettes — the effect-shaped FREE glyphs the card faces draw ` +
    `(shared table: mobile glyphShapes.ts · editor KwGlyph · this build script). Keywords stacked on one tile share a shape on purpose.</p>\n` +
    `<div class="glyph-grid">\n${tiles}\n</div>\n</section>`
  );
}

function buildCatalog() {
  const cards = load("cards");
  const enemies = load("enemies");
  const effects = load("effects");

  const body =
    `<h1>Catalog</h1>\n` +
    `<p class="muted">A single HTML document containing cards, enemies, effects, inline CSS, and inline JavaScript.</p>\n` +
    `<nav class="catalog-nav" aria-label="Catalog sections">` +
    `<a href="#cards">Cards (${cards.length})</a>` +
    `<a href="#enemies">Enemies (${enemies.length})</a>` +
    `<a href="#effects">Effects (${effects.length})</a>` +
    `<a href="#glyphs">Glyphs (${Object.keys(GLYPH_SHAPES).length})</a>` +
    `</nav>\n` +
    renderCards(cards) +
    "\n" +
    renderEnemies(enemies) +
    "\n" +
    renderEffects(effects) +
    "\n" +
    renderGlyphs() +
    "\n" +
    CATALOG_JS;

  writeFileSync(
    join(DEVLOG, "catalog.html"),
    page({
      title: "Axiomancer — Catalog",
      home: BACK,
      crumb: "Catalog",
      body,
      extraStyle: CATALOG_EXTRA_STYLE,
      footerNote: FOOTER,
    })
  );

  return { cards: cards.length, enemies: enemies.length, effects: effects.length };
}

const counts = buildCatalog();
console.log(`catalog: built single HTML catalog (${counts.cards} cards, ${counts.enemies} enemies, ${counts.effects} effects)`);
