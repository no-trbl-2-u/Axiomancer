{
  "name": "axiomancer-card-editor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local dev tool: web-based CARD (Action) editor for Axiomancer. Edits axiomancer-mechanics/src/Cards/cards.library.ts in place via the @mechanics alias. Local-only; not published.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "type-check": "tsc --noEmit",
    "lint": "eslint src",
    "verify": "npm run type-check && npm run lint && npm run test && npm run build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.3.4",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^3.2.4"
  }
}
